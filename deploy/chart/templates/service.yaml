apiVersion: v1
kind: Service
metadata:
  name: {{ include "harness-memory.fullname" . }}
  labels:
    {{- include "harness-memory.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
  selector:
    {{- include "harness-memory.selectorLabels" . | nindent 4 }}
